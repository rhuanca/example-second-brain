# Copy to .env and fill in. .env is gitignored.
#   cp env.example .env

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Your numeric Telegram user id (the only user the bot serves).
# Get it by messaging @userinfobot.
TELEGRAM_ALLOWED_USER_ID=

# Claude API key (provided later). Tests mock the LLM, so this can stay empty
# during development.
ANTHROPIC_API_KEY=

# Claude model to use for summaries.
ANTHROPIC_MODEL=claude-sonnet-4-6

# Optional: Supadata API key for YouTube transcripts (https://supadata.ai).
# When set, transcripts are fetched via Supadata's servers, which avoids the
# YouTube IP block that hits the local library on cloud/VPS hosts. Empty = use
# the local youtube-transcript-api.
SUPADATA_API_KEY=

# Web article extraction via Jina Reader (https://jina.ai/reader), which converts
# a page to Markdown AND captions its images with a vision model — so diagrams get
# summarized too. The caption feature requires a key (free at jina.ai). Without a
# key, articles fall back to local trafilatura (text only, no captions).
JINA_API_KEY=
# Set to false to disable Jina entirely and always use trafilatura.
JINA_ENABLED=true

# Absolute path to the dedicated Obsidian vault. PARA folders are created here.
VAULT_PATH=./vault

# --- Slack (optional) — the desk-side "ask" bot; capture stays on Telegram ---
# Only needed if you run the Slack bot (python -m second_brain.slack_main).
# See the README "Slack" section for how to create the app + get these.
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_ALLOWED_USER_ID=

# --- Knowledge base (optional) — MCP for agents + browse UI, read-only ---
# Only needed if you run it (python -m second_brain.kb.main / ./deploy-kb.sh).
# See DEPLOY.md for tokens, local use, and Cloudflare Tunnel + Access.

# Bearer tokens for /mcp, comma-separated, one per client (required to start).
# Generate: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
KB_AUTH_TOKENS=
# Where the embedding index and model live — outside the vault. To keep
# everything inside the project instead, use ./.kb-index (gitignored).
KB_INDEX_DIR=~/.local/share/second-brain-kb
# Local embedding model (fastembed, CPU/ONNX).
KB_EMBED_MODEL=BAAI/bge-small-en-v1.5
# Loopback only; anything else is refused. The tunnel connects locally.
KB_HOST=127.0.0.1
KB_PORT=8765
# Public hostname(s) behind the tunnel, e.g. kb.example.com (else MCP answers 421).
KB_ALLOWED_HOSTS=
# Cloudflare Access for the browse UI: team domain + application AUD tag (set both).
KB_CF_ACCESS_TEAM_DOMAIN=
KB_CF_ACCESS_AUD=
# Open the browse UI without Access — local/SSH use only. Ignored once Access is set.
KB_WEB_ALLOW_UNAUTHENTICATED=false
# Chat (/chat) uses ANTHROPIC_API_KEY above. Model defaults to ANTHROPIC_MODEL;
# set KB_CHAT_MODEL to use a different one just for chat (e.g. claude-opus-5).
KB_CHAT_MODEL=
# low | medium | high | xhigh | max — higher thinks harder and answers slower.
KB_CHAT_EFFORT=medium

# Optional: your Medium session cookie, to summarize member-only articles you pay
# for. Value = the `sid` cookie from medium.com (browser DevTools > Application >
# Cookies > medium.com > sid). Leave empty to only get free-post/teaser content.
MEDIUM_COOKIE=
